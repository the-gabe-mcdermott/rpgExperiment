#include <iostream>
#include <cstdio>

class TestSuite
{
    public:
        /*** RunAllTests ***
        * Prints status of tests to stdout.
        * Returns:
        *   Returns true on all tests pass.
        *   Returns false on any test failure.
        ***/
        bool RunAllTests(void);
};
